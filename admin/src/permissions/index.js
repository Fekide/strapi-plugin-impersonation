const permissions = {
  impersonate: [{ action: 'plugin::impersonation.impersonate', subject: null }],
  settings: [{ action: 'plugin::impersonation.settings', subject: null }],
}

export default permissions
